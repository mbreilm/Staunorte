import { IconZurueck } from "@/lib/icons";

/** Zurück-Pfeil für Zurück-/Abbrechen-Buttons (Detailseite, Fotogalerie, Erfassen-Flow, …). */
export function ZurueckPfeil() {
  return <IconZurueck size={20} aria-hidden="true" />;
}
